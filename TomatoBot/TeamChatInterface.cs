using System;
using System.Collections.Generic;
using System.Text;

namespace TomatoBot
{
    public class TeamChatInterface
    {
        public bool CanInitialize()
        {
            bool chatCanConnect = false;

            // Stubbed out for now -- originally interfaced with
            // Lync, but moving to MS Teams inteface
            return chatCanConnect;
        }
    }
}
